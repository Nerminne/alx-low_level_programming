#include <stdio.h>
/**
 * main - Entry point where the execution process takes place
 *
 * Return: Always 0, which means the program implemented successfully
 * 
 * void: means no output will return
 */
int main(void)
{
	return (0);
}
