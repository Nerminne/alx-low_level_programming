#include "main.h"
/**
 * add - return sum of two num
 * Return: Always 0 (Success)
 * @x: num1
 * @y: num2
 * main.h is header file contain prototype of functions used
 */
int add(int x, int y)
{
	return (x + y);
}
